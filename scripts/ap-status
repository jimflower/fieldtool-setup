#!/usr/bin/env bash
[ -f /opt/fieldtool/config.env ] && . /opt/fieldtool/config.env || true
set -euo pipefail
SSID=$(nmcli -t -f NAME con show --active | grep -Fx fieldtool-ap || true)
echo "=== AP Status ==="
nmcli dev status
echo
iw dev wlan0 info | sed -n 's/^/  /p'
echo
ip -br addr show wlan0
